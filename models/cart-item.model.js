class CartItem {
  constructor(quantity, productPrice, productTitle, productSum) {
    this.productQuantity = quantity;
    this.productPrice = productPrice;
    this.productTitle = productTitle;
    this.productSum = productSum;
  }
}

export default CartItem;